<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.medidoc">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>

        </activity>
        <activity android:name=".find_pswd" />
        <activity android:name=".join" />
        <activity android:name=".main_view" />
        <activity android:name=".sender_add" />
        <activity android:name=".sendlist" />
        <activity android:name=".set_mystatus" />
        <activity android:name=".setting" />
        <activity android:name=".find_id" />
    </application>

</manifest>